import { ControltouchedPipe } from './controltouched.pipe';

describe('ControltouchedPipe', () => {
  it('create an instance', () => {
    const pipe = new ControltouchedPipe();
    expect(pipe).toBeTruthy();
  });
});
